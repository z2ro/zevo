# Handoffs

Copie `TEMPLATE.md` para `<task-id>.md`. Reviews usam `<task-id>-review.md` e nunca sobrescrevem o handoff original. Relatórios são memória de execução, não substituem os contratos em `docs/` nem o estado em `graph/tasks.yaml`.
