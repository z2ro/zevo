# Zevo development

Trabalhe diretamente no MVP. Leia apenas o contrato, código e testes relacionados à mudança atual. Conteúdo e valores específicos de gameplay declarativo pertencem em `game_data/`; algoritmos e parâmetros estruturais permanecem em Python/`game_balance.py`.

Ignore `node_modules/`, `dist/`, `.git/`, `.venv/`, `__pycache__/` e caches. Depois de mudanças, execute primeiro os testes relacionados e então as validações finais do README.
